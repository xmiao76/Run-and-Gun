/**
 * Texture keys for the procedurally generated backdrop gradients.
 * Kept in a Phaser-free module so pure data modules (levelTheme) and tests
 * can reference them without pulling in the engine.
 */

/** Vertical dusk-sky gradient (jungle). */
export const SKY_TEXTURE = 'art/bg-sky';

/** Fortress interior backdrop gradient. */
export const FORTRESS_SKY_TEXTURE = 'art/bg-sky-fortress';
